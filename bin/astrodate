#!/usr/bin/env node
(function () {
  'use strict';

  var AstroDate = require('../lib/astrodate');
  var now = new AstroDate().toISOString();
  try {
    console.log(now);
  } catch (e) {
    process.stdout.write(now + '\n');
  }
  return now;
}());
